apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: my-java-app
  namespace: default
spec:
  imageRepositoryRef:
    name: my-java-app
  policy:
    semver:
      range: ">=1.0.0"